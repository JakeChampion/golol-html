# Measured and accepted behaviours

Behaviours that have been measured, written down, and pinned by a test. They are
not defects and must not be reported as new findings.

`Owner` says who could change it. `upstream` means the behaviour is lol-html's,
the version is pinned (W7), and the most a GIP can do is improve the test or the
documentation. `binding` means it is ours, it is accepted as it stands, and a
proposal to change it has to argue against the reason given.

Closing a row here, when the reason no longer holds, is a first-rate GIP. Adding
one is not: a row is added by the pull request that measured it, as part of the
change that pins it.

| # | Behaviour | Owner | Pinned by |
|---|---|---|---|
| B1 | Character references are never decoded on the way out. The href of `<a href="?a=1&amp;b=2">` reads as `?a=1&amp;b=2`. | upstream | `TestCharacterReferencesAreNotDecoded` |
| B2 | `SetAttribute` takes raw source text, not a literal value: only `"` is escaped, because only it would break the syntax. Content insertion with `Text` escapes fully. | upstream | `TestSetAttributeTakesRawSourceText` (properties) |
| B3 | A leading U+FEFF is dropped when an attribute value is read back. The value is serialised faithfully, and a U+FEFF in any other position survives. | upstream | `TestLeadingBOMIsStrippedOnRead` |
| B4 | Text chunk counts are not chunk-invariant: lol-html splits text at input chunk boundaries. That is the only thing that is not. Output, structural invocation counts, the number of text nodes and the text of each node are all the same however the input arrived. | upstream | `nodeinvariance_test.go` at every write size from 1 to 40, `examples/gip/chunkinvariance` over a larger corpus, and `FuzzRewrite`, which compares structural counts and per-node text over random input |
| B5 | Small writes cost a constant factor, not an asymptotic one: about 100 ns of crossing into C each, whatever their size, and no allocation. Releases up to v0.1.1 documented this as quadratic while an unclosed tag is buffered; it is not, and the buffered tag is the cheap case. | upstream | `bytecost_test.go`; the fuzz harness bounds input size and write count because of the factor |
| B6 | With `GracefulBailOut` false, exceeding `MaxMemory` delivers nothing to the sink: the response is broken. With it true, every input byte is preserved. | upstream | the memory-limit cases in `errors_test.go` |
| B7 | A handler panic does not unwind through Rust. It is caught at the boundary and re-raised on the goroutine that called `Write` or `Close`. | binding | the panic cases in `errors_test.go`. This is what made the v0.1.1 leak possible; the leak is fixed and the re-raise is kept |
| B8 | `withStream`'s own handle cleanup path is unreachable: the C API rejects only a NULL handler struct and the shim never passes one. Probed on v3.0.1, every `Stream*` method succeeds even on a void element. | binding | deliberately untested, recorded in SPEC.md |
| B9 | musl cannot be detected by build constraint, so it is selected by `-tags musl`. Getting it wrong fails at link time in both directions, loudly. | binding | `scripts/check-platforms.sh` |
| B10 | `-lunwind` is dropped on musl, because the c-api crate builds with `panic = "abort"` and Alpine does not ship libunwind. | binding | the Alpine job in `ci.yml` exists to falsify this; an undefined `_Unwind_*` there closes the row |
| B11 | `-lSystem -lc -lm` are dropped on darwin, because macOS links libSystem implicitly and naming it again makes `ld` warn on every consumer build. | binding | the darwin rows in `ci.yml` |
| B13 | Every selector-associated handler runs before every document-level handler for the same unit, whatever order the options were written in: `OnComment` before `OnDocumentComment`, `OnText` before `OnDocumentText`. The C API keeps the two in separate vectors, so the interleaving is lost before lol-html sees it. | upstream | `TestSelectorHandlersRunBeforeDocumentHandlers` |
| B14 | Removal suppresses output, not handler dispatch: handlers still run for content inside a removed element, and their edits are discarded. | upstream | `TestHandlersInsideRemovedContentStillRun` |
| B15 | `Remove()` then an inner insertion (`Append`, `Prepend`, `SetInnerContent`) emits the content without the element's tags; the reverse order discards it. `IsRemoved()` cannot guard it, being true after `RemoveAndKeepContent` too, where appending is well defined. Upstream's `should_remove_content` is `pub(crate)` and not in the C API, so the binding cannot tell the two removals apart. | upstream | `TestInsertionAfterRemoveIsStillEmitted`, `TestRemovalOrderAcrossTwoHandlers` |
| B16 | Neither `ContentType` is correct inside `<script>` or `<style>`: `Text` escapes `<>&` into raw-text content where references are not decoded, corrupting it; `HTML` is verbatim, so `</script>` in the content ends the element. Escaping it properly is a JavaScript transformation, not an HTML one. | upstream | `TestTextIntoRawTextElementsIsCorruptedNotDecoded`, `TestHTMLIntoAScriptCanEndTheElement` |
| B17 | `Text` escapes exactly `<`, `>` and `&`. A NUL passes through as a literal zero byte, so it does not survive a round trip through any parser. | upstream | `TestTextEscapesExactlyThreeCharacters` |
| B18 | The WHATWG encoding labels are aliases: `iso-8859-1`, `latin1`, `ascii` and `us-ascii` all select windows-1252, so bytes 0x80-0x9F decode as printable characters rather than controls. Required by the standard and what browsers do. | upstream | `TestEncoding/latin-1_labels_are_windows-1252` |
| B19 | Handlers always see UTF-8 whatever the document's encoding; inserted content is taken as UTF-8 and encoded on the way out, with unrepresentable characters becoming numeric character references. UTF-16 labels are refused, because the rewriter must find ASCII markup in the byte stream. | upstream | `TestEncoding/unrepresentable_inserted_characters_become_references`, `TestEncoding/utf-16*_is_rejected` |
| B20 | With `GracefulBailOut` false, a memory bail-out delivers nothing when the document arrived in one `Write` and a truncated rewritten prefix when it arrived in chunks. The cut lands on an element boundary, so the output is well-formed HTML. | upstream | `TestMemoryBailOutReachesTheSink`, `TestTruncatedBailOutLooksLikeAValidDocument` |
| B21 | The memory a rewrite needs depends on the write pattern: one measured document completes at `MaxMemory` 1024 in a single write and needs 8192 in 256-byte writes. | upstream | `TestMemoryNeededDependsOnHowTheInputIsFed` |
| B22 | Strict mode aborts on a text-content tag opening inside `<select>` (`title`, `style`, `iframe`, `xmp`, `plaintext`, `noembed`, `noframes`, `noscript`) or inside `<frameset>` (the same minus `noframes`, legal there). `<script>` is allowed in a select; `select`, `textarea`, `input` and `keygen` end the context. The abort is mid-stream, so the response is truncated. | upstream | `TestStrictModeTriggers`, `TestStrictModeFailureTruncatesTheResponse` |
| B23 | With strict off, content from an ambiguous tag to its closing tag - or to end of input if unclosed - is treated as raw text, so no handler runs for it. A sanitiser silently passes it through. | upstream | `TestLenientModeHandsContentPastTheHandlers`, `TestLenientModeMissedRegionEndsAtTheClosingTag` |
| B24 | Without `WithESITags`, an `esi:` element is an ordinary container: its content runs to the next matching end tag, so replacing or removing an unclosed `<esi:include>` swallows the enclosing element's end tag. A trailing slash does not help - HTML ignores it on an element that is neither void nor foreign. | upstream, opt-in | `TestESITags` |
| B25 | Allocations per rewrite are `base + k x matches`, with base independent of document length: 1 per unit wrapper, 1 per string crossing the boundary, 0 for a `SourceLocation`, 4 per attribute for `AttributeList`/`Attributes`. Nothing is cached. | binding, gated | `alloc_test.go` |
| B26 | A `StreamFunc` runs when its content is emitted: it cannot see anything not yet parsed, and it is skipped entirely if the content is discarded (the element or an ancestor was removed). | binding/upstream | `TestStreamFuncCannotSeeLaterContent`, `TestStreamFuncIsSkippedWhenItsContentIsDiscarded` |
| B27 | Inserted content is emitted verbatim and never dispatched to any handler, including the one that inserted it. No loop hazard; accumulators are safe; a sanitiser does not sanitise its own insertions. | binding/upstream | `insertion_test.go` |
| B28 | Every insertion goes immediately adjacent to the unit, so successive `After` and `Prepend` calls come out reversed while `Before` and `Append` come out in call order. | upstream | `insertorder_test.go` |
| B29 | `OnText` fires for descendants' text, and `IsLastInTextNode` ends a text node rather than the element's content. For an element's whole text, accumulate and finish at `Element.OnEndTag`. | upstream | `textnode_test.go` |
| B30 | A comment handler fires for bogus comments: `<?php ... ?>`, `<?xml ... ?>`, `<!anything>`. The `<?` forms keep their `?` in the text; `<!x>` has the same text as `<!--x-->`, so only `SourceLocation` against the input tells them apart. | upstream | `comment_test.go` |
| B31 | An attribute-only rewrite never changes the tree an independent parser sees, including the parts error recovery relocated. | binding, gated | `properties/attributes_test.go` |
| B32 | `:not()` with a compound selector negates each part separately and requires all of them: `:not(div.a)` behaves as `:not(div):not(.a)`, and `:not(div.a, span.b)` matches nothing. Correct only with a single simple selector. | upstream | `TestNotWithACompoundSelectorIsWrong` |
| B33 | The supported selector subset is everything decidable at the start tag; `:last-child`, `:only-child`, `:empty`, `:nth-last-*`, the sibling combinators, `:is`/`:where`/`:has`, pseudo-elements and explicit namespaces are rejected. | upstream | `TestSupportedSelectors`, `TestUnsupportedSelectors` |
| B34 | Selector matching is decided against the document as it arrived, before any handler runs: an edit never changes which handlers fire, in either direction. Handlers do see each other's edits through `Attribute`. | binding/upstream | `TestSelectorsMatchTheDocumentAsItArrived` |
| B35 | Registering a selector costs about five allocations at `NewWriter`, one fewer for a repeat of one already registered. Matching cost grows with the number registered, on every element - there is no index by tag or class. | binding, gated | `TestRegisteringSelectorsCostsLinearly` |
| B36 | Markup built by the caller and inserted as `HTML` is the only path that escapes nothing. A document-derived value in a hand-built attribute is an injection; use `EscapeText`/`EscapeAttribute`, or `SetAttribute` where there is an element to hold. | binding | `TestHandBuiltAttributesAreNotEscapedForYou`, `escape_test.go`, `properties/escape_test.go` |
| B37 | `SetAttribute` rewrites the double quote and nothing else: its argument is raw attribute-value source, the mirror of what `Attribute` reports, so `&amp;`, `<`, `>` and `'` pass through. Writing the five characters `&amp;` therefore sets a value a parser reads as one `&`. | binding | `TestWhatSetAttributeEscapes` |
| B38 | Character references are not decoded inside comment data, so escaping does not protect a comment: `&gt;` stays four characters. What ends a comment is two hyphens, and a comment may not begin with `>` or `->` - `<!-->` and `<!--->` are empty comments with the rest left as text. | upstream | `examples/gip/linktext` `TestCommentSafeSurvivesAReparse` |
| B39 | `DocumentEnd.Append` appends to the end of the output, which is where the input stopped. An input cut off inside a script, style, textarea, title, comment or doctype swallows the appended markup as text: measured, seven of twelve mid-construct documents produce no element. Cut off inside a start tag is worse - the unterminated attribute absorbs bytes to the next quote and the remainder becomes attributes of that element. `Write` and `Close` both succeed and `WithStrict` changes nothing. | upstream | `docend_test.go` |
| B40 | `Element.OnEndTag` on `body` does not fire when `</body>` is absent, which is legal HTML. So "insert before `</body>`" silently does nothing on such a document, and there is no signal distinguishing that from a document with no body. | upstream | `TestABodyWithoutAnEndTagNeverCallsOnEndTag` |
| B41 | An `EndTag`'s `Before`/`After` content is emitted even if the end tag is removed, in either order - unlike `Element.Remove`, whose interaction with insertions is order-dependent. | upstream | `TestEndTagContentSurvivesRemovingTheEndTag` |
| B42 | A selector matches a tag name in any namespace: `a[href]` matches HTML, SVG and MathML anchors, `title` matches a document title and an SVG tooltip. There is no namespace syntax in the subset. | upstream | `TestASelectorMatchesEveryNamespace` |
| B43 | `NamespaceURI` reports the namespace an element's *children* are parsed in, not the element's own. The integration points therefore report HTML despite being foreign elements: SVG's `foreignObject`, `desc`, `title`; MathML's `mi`, `mo`, `mn`, `ms`, `mtext`; and `annotation-xml` when its `encoding` is `text/html` or `application/xhtml+xml`. So it cannot separate a document title from an SVG tooltip - both report HTML. | upstream | `TestNamespaceURIReportsTheContentNamespace`, `TestAnnotationXMLDependsOnItsEncoding` |
| B44 | `head title` and `head > title` match only when `<head>` is in the source, and `<head>` is optional: given `<title>x</title><p>y</p>` they match nothing. A selector cannot express "not inside svg" either, so excluding foreign content needs a depth counter in the handler. | upstream | `TestContextSelectorsNeedTheContextToBePresent` |
| B45 | `OnDoctype` fires for every `<!DOCTYPE>` token, including ones an HTML parser discards: after an element, after text, inside `<html>`, or a second one. Against x/net/html, four of six such documents diverge. So "a doctype was seen" is not "this page has a doctype". | upstream | `differential/doctype_test.go` |
| B46 | `Doctype` has no insertion methods - the C API has none - so a doctype cannot be added or replaced, only removed. Prefixing the destination before the rewriter is the only route and is wrong for input that already has one, which would then be second and discarded. | upstream | `differential/doctype_test.go` |
| B47 | With no `<head>` element in the source, inserting before `<body>` puts content in the head a parser builds; inserting before the first element of a fragment does too, unless text came first, which has already opened the body. | upstream | `differential/head_test.go` |
| B48 | Attribute *values* are matched case-insensitively only for the 46 attributes on the HTML specification's list (`rel`, `type`, `lang`, `target`, `method`, `checked`, ...) and exactly for everything else, including `id`, `class`, `href`, `src`, `name`, `title`, `content` and every `data-*`. The `.cls` and `#id` shorthands are exact too. Verified against the full list plus a control group. | upstream, gated | `TestAttributeValueCaseFollowsTheHTMLList` |
| B49 | The `i` and `s` flags override that default in both directions and work with every attribute operator, so `[name="foo" i]` matches `name="Foo"` and `[rel="canonical" s]` does not match `rel="CANONICAL"`. | upstream | `TestTheCaseFlagsOverrideTheDefault` |
| B50 | A character the document's encoding cannot represent is inserted as a numeric character reference. That is decoded in text and in attributes, and not in raw text: inserting `日` into a `<script>` in a windows-1252 document puts the eight characters `&#26085;` in the script. No error, and the `ContentType` makes no difference because the substitution happens after escaping. A `textarea` or `title` is escapable raw text and is fine. | upstream | `encoding_test.go` |
| B51 | Handler-visible strings are always UTF-8, whatever the document's encoding. A misdeclared encoding changes what handlers see - the same input read as utf-8 and as windows-1252 gives `café` and `cafÃ©` from one attribute - and corrupts the output only when a text handler is registered: text is decoded and re-encoded then, so a byte invalid in the declared encoding becomes U+FFFD on the way out even if the handler does nothing. With no text handler the bytes pass through. | upstream | `TestHandlersAlwaysSeeUTF8`, `TestAMisdeclaredEncodingChangesWhatHandlersSeeAndNotTheOutput`, `TestInvalidBytesUnderAMisdeclaredEncodingReachTheOutput` |
| B60 | Nothing is sniffed: a document's own `<meta charset>` is ordinary markup and never consulted, so the label passed to `WithEncoding` is the only encoding the rewriter knows. Inserting a charset meta does not change the output bytes either, so one naming a different encoding produces a document that lies about itself. | upstream, gated | `encoding_test.go` |
| B52 | An insertion can only go at a position the rewriter has not passed, so a rewrite whose content depends on evidence appearing after the position it writes to is not a one-pass rewrite at all. Head content derived from the body is the common case, and nothing reports it: the handler either never runs or runs before it knows what to say. | binding/upstream | `docs/gip/GIP.md` rules; `examples/gip/pagenav` |
| B53 | A second pass roughly doubles the fixed allocation count - 27 for one pass against 57 for two, a ratio flat in document size, since almost all of it is building the rewriter. What grows with the document is the buffer the first pass needs. | binding, measured | `examples/gip/pagenav` doc comment |
| B54 | Raw text ends at `</` plus the tag name, compared without regard to case, followed by `>`, `/`, ASCII whitespace, or the end of the inserted content. `</scriptx` does not end a script; `</script foo>` does. Checked against x/net/html across four elements and eight terminators, with no divergence. | upstream, gated | `rawtext_test.go`, `differential` probe |
| B55 | Inserting into the content of a `script`, `style`, `textarea` or `title` with `ContentType` `HTML` is refused with `ErrRawTextBreakout` when the content would close that element. `Before`, `After` and `Replace` write outside the element and are not checked, and neither is `Text`, which escapes the `<`. Inserting a *whole* `<script>` element as markup cannot be checked, since its payload legitimately contains its own closing tag. | binding, gated | `rawtext_test.go` |
| B56 | `SourceLocation` offsets are absolute across the whole stream and identical however the input is chunked, so slicing a caller's own copy of the input at them returns the unit. Text chunk *boundaries* do move with the writes - that is the documented split - but their locations stay absolute, contiguous and in order. | binding, gated | `sourceloc_test.go`, `FuzzRewrite` |
| B57 | A repeated attribute is kept by the parser, though the HTML specification requires all but the first to be dropped. The API is split: selectors, `Attribute`, `HasAttribute` and `SetAttribute` act on the first copy, `Attributes` and `AttributeList` yield every copy, and `RemoveAttribute` removes every copy. `[a="v"]` therefore does not match `<p a="x" a="v">`. | upstream/binding, gated | `duplicate_test.go` |
| B58 | Options are last-writer-wins per field, except `WithMemorySettings`, which takes a whole struct and replaces every field in it. `WithGracefulBailOut` is therefore kept separately and combined by union, so the two compose in either order; before that fix, `WithGracefulBailOut()` before `WithMemorySettings(...)` was silently discarded and a bail-out threw away the output produced so far. | binding, gated | `options_test.go` |
| B59 | `go vet ./...` stops at a module boundary, so one invocation covers one of the three modules. `properties/` was tested from the day it landed and never vetted, in CI or in `make lint`. | binding, gated | `scripts/check-modules.sh` |
| B61 | `PreallocatedParsingBuffer` counts against `MaxMemory` and does not lower the peak, so raising it raises the smallest limit that completes, roughly one for one: 0/16/1024/4096/8192 preallocated needs 832/848/1856/4928/9024. No document tried was cheaper with a buffer, including ones chosen to reallocate. Equal values are accepted by `validate` and fail as soon as a selector has to match. | binding, gated | `memory_test.go` |
| B62 | No SVG tag-name adjustment is applied. A parser maps an SVG start tag to its canonical spelling, so a browser's DOM holds `linearGradient` however the page wrote it; here `TagName` gives the lower-cased name and `TagNamePreserveCase` gives the source spelling, so neither is canonical unless the page happened to write it that way. The output keeps the source spelling and a browser adjusts it, so nothing is wrong on the way out. | upstream, gated | `differential/tagname_test.go` |
| B63 | A sink's methods report acceptance, not delivery. After the destination fails the sink keeps accepting everything - fifty writes measured, none refused - and the error surfaces only from `Write` or `Close`. `Sink.Err` is the only way a `StreamFunc` can see it and stop. | binding, gated | `sink_test.go` |
| B64 | A boolean attribute cannot be written bare: `SetAttribute("defer", "")` emits `defer=""` and the C API takes no other shape. Semantically identical - presence is the meaning - but it does not match the spelling a page used. A bare attribute in the input passes through unchanged and reads back as an empty value. | upstream | `examples/gip/deferscripts` `TestABooleanAttributeIsWrittenWithAValue` |
| B65 | `IsSelfClosing` is about how the tag was *written*, not what the element can hold. In HTML a trailing slash is ignored by the parser and still reported: `<div/>` reports self-closing and goes on to have content and an end tag. `CanHaveContent` is the test for "may this hold content" and is right in every case. In foreign content the two agree, because there the slash is what closes the element. | upstream, gated | `selfclosing_test.go` |
| B66 | A value decoded from raw attribute source has to be escaped before being written back with `SetAttribute`, which takes source: `?a=1&amp;lt=2` decodes to `?a=1&lt=2`, and writing that raw reads back as `?a=1<=2`, because `&lt` is a reference with or without its semicolon. | upstream | `examples/gip/modulesplit` `TestARawSourceValueSurvivesTheCopy` |
| B67 | Ten element names hold content an HTML parser does not read as markup, not four: `script`, `style`, `textarea`, `title`, `iframe`, `noembed`, `noframes`, `noscript`, `xmp` and `plaintext`. Measured by asking the parser about every element name in the HTML index - an element inside one of those is not reported as an element. | upstream, gated | `rawtext_test.go` `TestTheGuardCoversEveryRawTextElement` |
| B68 | `plaintext` is raw text that cannot be closed: everything after the start tag is its content, to end of input, so its own closing tag is text. `CanHaveContent` is true and yet there is no end tag, so `OnEndTag` returns nil and never runs, and `Append` is dropped without error. `Prepend` and `SetInnerContent` work. | upstream, gated | `rawtext_test.go` `TestPlaintextCannotBeBrokenOutOf` |
| B69 | Character references are decoded only inside `textarea` and `title`. In the other seven - `script`, `style`, `iframe`, `noembed`, `noframes`, `noscript`, `xmp` - `ContentType.Text` inserts a literal `&lt;`, so it is safe and wrong: nothing breaks out and the content no longer says what it said. Measured against `golang.org/x/net/html`. | upstream, gated | `differential/rawtext_test.go` `TestWhichRawTextElementsDecodeReferences`; `rawtext_test.go` `TestTheErrorSaysWhatToDoInstead` |
| B70 | The raw-text guard is by tag name and does not consider namespaces, so it also refuses an insertion into `<svg><title>`, where the content is markup. Conservative rather than wrong: an inserted `</title>` still ends that element, by tree construction rather than by the tokenizer. `Text` is the way through. | binding, gated | `rawtext_test.go` `TestForeignContentIsRefusedConservatively` |
| B71 | The guard covers insertions into content through `Element` and `EndTag` only. `TextChunk.Before`, `After`, `Replace` and every streaming insertion are unchecked, so a text handler editing a script can break out silently. A text chunk has no way to name the element it is inside, and a streaming write can split a closing tag across two calls, so neither has anything to look up. | binding, gated | `rawtext_test.go` `TestWhatIsStillNotChecked` |
| B72 | An element whose end tag the source omits ends, for this library, at the *enclosing* element's end tag. Every operation positioned at the element's end therefore acts on a range that is not the element: on `<ul><li>a<li>b<li>c</ul>`, `Append` and `After` keep one of three insertions, `SetInnerContent` deletes items b and c, `Replace` deletes all three, and `Remove` on the first item alone yields `<ul>`. `Prepend` and `Before` are correct, because they are positioned at the start tag. Nothing errors. The same program on explicit end tags is correct in every row. | upstream, gated | `endtagposition_test.go`; `differential/endtag_test.go` |
| B176 | `EndTag.Before` is the exception to B72's end-of-element rule: it is misplaced like the rest but it does not lose content. On the same `<ul><li>a<li>b<li>c</ul>`, `Append` per item gives `<ul><li>a<li>b<li>c[1]</ul>` and `EndTag.Before` per item gives `<ul><li>a<li>b<li>c[3][2][1]</ul>` - all three insertions, innermost first, because all three handlers run at the one `</ul>`. With the end tags spelled out the two are identical and per-item. So where either would do, `EndTag.Before` turns a silently dropped insertion into a misplaced one. | upstream, gated | `examples/gip/shadow` `TestAppendAndEndTagBeforeDoNotAgreeWhenTheEndTagIsOmitted` |
| B73 | `OnEndTag`'s handler runs against the tag that closed the element, which for an omitted end tag is a different element's: in `<ul><li>a<li>b</ul>` both items' handlers run at `</ul>`, innermost first, and `EndTag.Name` reports `ul`. If nothing closes the element - `<p>a<p>b` at the top level - the handler does not run at all. Comparing `EndTag.Name` with the element's tag name separates the two, verified against what the source spells at that position over sixteen shapes. | upstream, gated | `endtagposition_test.go` |
| B74 | The extent recipe on `Element.SourceLocation` - start tag Start to end tag End - measures to the end of the enclosing element for an implicitly closed element. Both items of `<ul><li>a<li>b</ul>` measure 10 and 15 bytes where the true extents are 5 and 5. | upstream, gated | `endtagposition_test.go` `TestTheExtentOfAnImplicitlyClosedElementIsNotItsOwn` |
| B75 | `srcset` is not a comma-separated list. The algorithm collects the URL up to whitespace and only treats a comma as a separator when the URL ends with one, so `srcset="/a.png,/b.png"` is a single reference to a file whose name contains a comma. Splitting on commas invents URLs that are not there, which matters for data: URLs and for query strings. | upstream (HTML) | `examples/gip/weight` `TestParseSrcset` |
| B76 | There is no top-level end-tag handler. The only way to observe an end tag is `Element.OnEndTag`, which is per element and fires against whatever tag closed it, so one `</ul>` reaches a program once per element it closes. Deduplicating by `EndTag.SourceLocation().Start` turns that back into one event per token. | binding/upstream, gated | `examples/gip/depth` |
| B77 | A depth counter driven by tokens over-counts by one per implicitly closed element: `<ul>` with forty implicit `<li>` reports 41 where the tree is 2. Counting elements rather than tags means implementing the specification's implied end tags in the caller. | upstream, gated | `examples/gip/depth` `TestATokenCounterDrifts` |
| B78 | An output-size budget cannot fail earlier than the rewriter emits, and with a handler registered one enormous token holds everything: measured, a 200,006-byte document that is a single unterminated start tag produces no output until all of it is consumed. Every other shape measured emits within the first 4 KiB, and with no handlers registered even the unterminated tag does. So an output budget is not a memory bound; `MaxMemory` is. | upstream, gated | `examples/gip/sizebudget` `TestHowLateOutputCanBe` |
| B79 | A graceful bail-out's flush is not bounded by `MaxMemory`: measured, a 1024-byte limit flushed 4096 bytes in one write. A budget enforced at the destination still holds. | upstream, gated | `examples/gip/sizebudget` `TestAGracefulFlushIsStillBoundedByTheOutputBudget` |
| B80 | An element's own namespace is the namespace its *parent's* children are parsed in, which is what `NamespaceURI` reports for the parent - so a stack of those values answers it for every element. Counting `<svg>`/`<math>` depth instead is wrong at the integration points: the `<p>` in `<svg><foreignObject><p>` is an HTML paragraph and a counter calls it SVG. `<svg>` and `<math>` are the exception, being themselves foreign. | binding, gated | `examples/gip/histogram` `TestIntegrationPointsSwitchBack` |
| B81 | One element crossing costs about one allocation before the handler does anything, because the `Element` wrapper is heap-allocated per match: measured 625 allocations for the ~604 elements of `benchDoc(200)`, against 14 for the whole passthrough rewrite. Each string read adds one more per element - `TagName` takes it to 1229 - while `CanHaveContent` and `NamespaceURI` add none. | binding | `BenchmarkCrossing` |
| B82 | No selector reaches text or comments that are not inside any element, so `OnText("*")` and `OnComment("*")` are not "all of it". Measured: `hello` gives 0 selector text calls and 2 document ones; `before<p>a</p>after` gives 2 and 6. A full document hides it, because `<html>` encloses everything. `OnDocumentText` and `OnDocumentComment` are the ones that see everything. | upstream, gated | `handlercalls_test.go` |
| B83 | The final chunk of a text node is a call of its own and carries no bytes - measured empty in nine shapes including a 100 KB node and all four raw-text elements, and under one-, two-, three-, five- and whole-document writes. So a text handler runs at least twice per text node and about half its calls on prose are handed nothing. An element with no text produces no calls at all. | upstream, gated | `handlercalls_test.go`; `examples/gip/handlerstats` |
| B84 | An `Option` is opaque once built, so a handler cannot be wrapped, counted or introspected from outside. Instrumentation has to replace the constructors, and an end-tag handler - registered from inside a running handler - has no `Option` at all and can only be counted if the caller routes it through the instrumentation deliberately. | binding, gated | `examples/gip/handlerstats` |
| B85 | How many times the destination is written to is decided by the rewrite, not the document. A mutated start tag is re-serialised piece by piece - `<`, name, space, each attribute, `="`, value, `"`, `>` - so one `SetAttribute` turns one write into twelve on a single element, and 2000 elements turn one 132 KB write into 22,001 writes of median size one byte. Passthrough of the same document in one `Write` is one write. Nothing is buffered on purpose; an unbuffered destination wants a `bufio.Writer`. | upstream, gated | `writecount_test.go`; `examples/gip/rollinghash` |
| B86 | Input write sizes shape output write sizes: the same 132 KB document fed in 64-byte writes produces 2063 destination writes under passthrough, 33 in 4096-byte writes, and 1 in a single write. | upstream, gated | `writecount_test.go` `TestSmallInputWritesMakeSmallOutputWrites` |
| B87 | A UTF-8 sequence left open when a `StreamFunc` returns is dropped by lol-html: `WriteChunk("ab\xc3")` produced `ab` with no error. A `WriteString` while one is open does not join it either - the held bytes become U+FFFD and the string follows. Both are now `ErrIncompleteRune`. Splitting a rune across `WriteChunk` calls is fine and always was. | binding, gated | `incompleterune_test.go`; `properties/sink_test.go` |
| B88 | Passthrough is byte-identical over 63 documents and 2815 comparisons - every write pattern, and every mode from no handlers to all seven registered - with two exceptions: a text handler rewrites bytes that are not valid in the declared encoding to U+FFFD, and strict mode refuses an ambiguous parse rather than emitting anything. | upstream, gated | `examples/gip/passthrough` |
| B89 | Transforming text and writing it back has three obvious spellings and two are wrong. `Replace(f(Text()), Text)` escapes references that were already escaped, on the first pass: `caf&eacute;` becomes `caf&amp;EACUTE;`. `Replace(f(Text()), HTML)` is stable and corrupts differently - the transform ran over the source, so `&eacute;` became `&EACUTE;`, which is not a reference - and it is HTML, so a data-driven transform is an injection. Only `Replace(f(html.UnescapeString(Text())), Text)` is right on the first pass and unchanged by the second. | binding, gated | `textroundtrip_test.go` |
| B90 | Idempotence is a property of the rewrite, not of the library. `SetAttribute`, `RemoveAttribute`, `SetTagName`, `SetInnerContent` and `Remove` are idempotent by construction; `Append`, `Prepend`, `Before` and `After` are not and need a guard, and the guard has to be readable at the start tag - an insertion can only go where the rewriter has not been, so a guard that looks for the previously inserted content is looking past the position it protects. | binding, gated | `examples/gip/idempotent` |
| B91 | A text chunk never contains part of a character, measured at one byte per write over two-, three- and four-byte runes, in text, in a comment and in an attribute value. `Sink.WriteChunk` has the opposite rule for content going in. So a per-chunk transform is safe per character and wrong per pattern: `strings.ToUpper` is fine, a regexp for a word is not. | upstream, gated | `examples/gip/chunkinvariance`; `chunkboundary_test.go` |
| B92 | Everything a handler can observe is invariant across write patterns except how text is divided into chunks: element, comment and doctype calls and their order, tag names, attributes, source locations, end tags, and the text of each node are identical however the input arrived. Measured over 22 documents and seven write patterns, 154 comparisons. The text handler's call count moves by up to 500x on the same document. | upstream, gated | `examples/gip/chunkinvariance` |
| B93 | Building a rewriter costs about 13 allocations with no handlers and around seven more per distinct selector, falling as more are registered because the slices behind them grow in steps. A repeated selector costs about one and a half fewer, since each distinct selector is parsed once and reused. The `# Cost` section said five and one; nothing measured it. | binding, gated | `alloc_test.go` `TestSelectorRegistrationCost` |
| B94 | A character reference can be split across text chunks - `&amp;` arriving as `&am` and `p;` - so `html.UnescapeString` applied per chunk decodes neither piece. Decoding has to happen after accumulating the node. This is the "safe per character, wrong per pattern" rule in its most common form, since decoding is the most common per-chunk operation. | upstream, gated | `examples/gip/plaintext` `TestAReferenceSplitAcrossChunksNeedsAccumulating` |
| B95 | An end-tag handler has three timings, not two. At its own end tag it fires exactly where the element ends; at an ancestor's end tag it fires exactly where the element ends if that ancestor's end tag is what closed it, and *after* where it ends if a sibling's start tag was - in `<ul><li><em>a<li>b</ul>` the `em`'s callback arrives after `b` was reported. Nothing in the callback separates the last two, so anything accumulating has to keep its own stack. | upstream, gated | `endtagposition_test.go` `TestTheThreeTimingsOfAnEndTagHandler`; `examples/gip/markdown` |
| B96 | An implied end tag closes everything inside the element it closes, not just that element: the second `<li>` of `<ul><li><em>a<li>b</ul>` ends the `em` too. A caller modelling this has to pop through to the element being closed rather than test the top of its stack, with a barrier per rule so a malformed document does not unwind everything. | upstream (HTML), gated | `examples/gip/markdown` `TestEmphasisDoesNotLeak` |
| B97 | A colon in a tag or attribute name must be escaped in a selector: `esi\:include`, not `esi:include`, and `[xlink\:href]`, not `[xlink:href]`. Unescaped, the colon is read as a pseudo-class, so the error is "Unsupported pseudo-class or pseudo-element in selector" - naming something the caller did not write. `SelectorError` now adds the answer when the rejected selector holds an unescaped colon. | upstream, gated | `selector_test.go` `TestAColonOrDotInANameHasToBeEscaped` |
| B98 | A dot in a class or id name must be escaped too, and that one does not fail: `.a.b` parses as "class a and class b" and matches nothing, so the handler simply never runs. `.a\.b` matches `class="a.b"`. A hyphen needs no escape. | upstream, gated | `selector_test.go` `TestAColonOrDotInANameHasToBeEscaped` |
| B99 | Counting words per text chunk over-counts, because a chunk boundary can fall inside a word: a 200-word document written a byte at a time counts as over 400 with `strings.Fields` per chunk. A word also spans inline markup - `<p>three<b>four</b></p>` is one word - so the state has to survive both a chunk boundary and an element boundary, and be cleared only by blocks. | upstream, gated | `examples/gip/readingtime` `TestPerChunkCountingOvercounts` |
| B100 | `Element.SetUserData` was documented as being read "most usefully" by an end-tag handler, which is impossible: `EndTag` has no user data - lol-html provides it for elements, comments, text chunks and the doctype only - and the element is detached by the time its end-tag handler runs, so reading through the captured `Element` returns nil. What it is actually for is a second handler given the same unit: two selectors matching one element, or an `OnComment` and an `OnDocumentComment` on one comment. | binding, gated | `userdata_test.go` |
| B101 | User data is per unit, not per position: two elements never share it, and a value set on one text chunk is not readable from the next chunk of the same text node - so it is not a place to accumulate across chunks, which is the documented pattern for text. | upstream, gated | `userdata_test.go` `TestUserDataIsPerUnit` |
| B102 | `HandlerError.Kind` has seven values, not the six documented: `element`, `comment`, `text`, `doctype`, `document-end`, `end-tag` and `streaming`. The last is a `StreamFunc`'s own failure, reported separately because it runs later than the handler that registered it. | binding, gated | `handlererror_test.go` `TestEveryDocumentedKindIsReachable` |
| B103 | `HandlerError.Selector` used to be empty for an end-tag or streaming handler, although both are registered from a handler that had one. They now inherit it, so a failure says which selector it belongs to rather than only which kind it was. | binding, gated | `handlererror_test.go` `TestHandlerErrorKindAndSelector` |
| B104 | There is no way to tell a streaming rewrite to stop; the only stop is a handler returning an error, which poisons the `Writer` and truncates the output. For an extractor that wants text rather than a document that is the right trade, and what it saves is bounded by the read buffer rather than by the data: the rewriter stops at the first write after the handler fails, so a 150 KB document whose summary ends at byte 19 reads exactly one buffer. | binding, gated | `examples/gip/summary` `TestStoppingEarlyBoundsWhatIsRead` |
| B105 | Content a parser moves out of a table - text between `<table>` and the first `<tr>`, text inside a `<tr>` but outside a cell, an inline or block element in either place - is reported *inside* the table here, because there is no tree to move it in. The output is byte-identical, since a browser fosters it out again, so nothing looks wrong. Measured against `x/net/html` in five shapes. | upstream, gated | `differential/table_test.go` |
| B106 | So `Element.Remove` on a table removes fostered content that a tree-based edit keeps: `<p>before</p><table>stray<tr><td>a</table><p>after</p>` becomes `<p>before</p><p>after</p>` here and `<p>before</p>stray<p>after</p>` in a tree. A text handler on the table is given the fostered text too, which makes "the text of this table" the wrong question to ask that way. | upstream, gated | `differential/table_test.go` `TestRemovingATableTakesFosteredContentWithIt` |
| B107 | `SetTagName` can change what an element's content means, because whether content is markup is decided by the element it is in. Renaming `script`, `style`, `textarea` or `title` to an ordinary element turns its text into markup - `<script>var x = "<img src=x onerror=alert(1)>"</script>` renamed to `div` yields a live image - which is the hazard `ErrRawTextBreakout` refuses for an insertion, reached without inserting anything. The other direction turns markup into text. The call happens at the start tag, before any content is reported, so the library cannot see it coming. | upstream, gated | `settagname_test.go` |
| B108 | `ErrDetached` said it is returned by "any method" on a unit used after its handler returned. Only mutators return it. Every getter answers with a zero value and no error - `TagName` `""`, `CanHaveContent` false, `SourceLocation` `{0,0}`, `Attribute` `("", false)`, `Attributes` no iterations - so a detached unit gives plausible answers. `Remove` and `ClearEndTagHandlers` return nothing at all, having no error to give. | binding, gated | `detached_test.go` |
| B109 | `HasAttribute` is the only getter that reports detachment, because its signature has room for an error. So `Attribute` cannot tell "the attribute is absent" from "the element is gone" and `HasAttribute` can - an accident of signature rather than a design, and the reason `Detached()` exists. | binding, gated | `detached_test.go` `TestAnAbsentAttributeAndADetachedElementLookTheSame` |
| B110 | A second pass roughly doubles the allocation count at every document size - measured 24/52 at 2 elements, 45/98 at 40, 425/866 at 800 - and the reason the `# Cost` section gave was wrong. It said the ratio holds "since almost all of it is building the rewriter"; at 800 elements one pass costs 425 allocations, so the per-element work dominates and the second pass simply does all of it again. Two passes over a large document cost twice rather than being amortised. | binding, gated | `alloc_test.go` `TestASecondPassCostsTwice` |
| B111 | `Comment.SetText` refuses a comment-closing sequence rather than escaping one; its documentation said the value "is escaped ... so untrusted input is safe". Nothing breaks out either way, but the call fails, so a caller passing arbitrary text gets a failed rewrite rather than a sanitised comment. Measured: `-->`, `--!>`, `->`, `a-->b`, `a--!>b` and `<!-->` refused; `--`, `--!`, `<!--` and `a--b` accepted, each round-tripping as one comment. | binding, gated | `commenttext_test.go` |
| B112 | A comment built by hand out of `HTML` content has no guard, and there is no escaper for it: `Append("<!-- "+title+" -->")` with a title containing `-->` or `--!>` lets an element out. `EscapeText` prevents the breakout and changes what the comment says, since a comment holds characters rather than references - so the choice is a wrong comment or a dangerous one, which is why `SetText` refuses instead. | upstream, gated | `commenttext_test.go` `TestAHandBuiltCommentHasNoGuard`; `differential/comment_test.go` |
| B113 | `ContentType.Text` cannot add a tag - checked as a property over every generated document, value and insertion position, for the plain and streaming paths and for `EscapeText` by hand - and can still change the tree a browser builds. Tree construction responds to the presence of text: `<p><a><div></div></a></p>` becomes `<p><a></a></p><div></div><p></p>`, and with one character in the div it becomes `<p><a></a></p><div><a></a></div><p></p>`, gaining an `<a>` the markup does not contain. Well-nested documents are unaffected. | upstream, gated | `properties/text_structure_test.go`; `differential/textstructure_test.go` |
| B114 | `SetAttribute` writes the first copy of a duplicated attribute and leaves the rest - `<a href="first" href="second">` set to `safe` becomes `<a href="safe" href="second">` - while `RemoveAttribute` takes every copy and documents that it does. So a rewrite that sanitises by changing a value leaves the value it was sanitising in the bytes. Remove and re-set is the fix, at the cost of the attribute's position. | binding, gated | `attrmutation_test.go` |
| B115 | Mutating an element while iterating its attributes is safe, and the walk is over the attributes as they were: setting or removing inside the loop takes effect and does not disturb it, and an attribute added inside the loop is not visited - measured by adding one per iteration and terminating at the original count. | upstream, gated | `attrmutation_test.go` `TestMutatingWhileIterating` |
| B116 | `EscapeAttribute` is not what `SetAttribute` applies, although the `# Cost`-adjacent section on building markup called them the same thing. `SetAttribute` escapes the double quote alone, because the library writes the quotes; `EscapeAttribute` escapes `"`, `'`, `<`, `>` and `&`, because hand-built markup might use either quote and a bare `&` could begin a reference. So a value read from a document round-trips through `SetAttribute` and gains an escape through `EscapeAttribute`. | binding, gated | `escape_test.go` `TestEscapeAttributeIsNotWhatSetAttributeApplies` |
| B117 | `RemoveAndKeepContent` on one of the ten raw-text elements turns its content into markup: unwrapping `<script>var x = "<img src=x onerror=alert(1)>"</script>` leaves a live image. Measured for all ten. This is the third door into the `ErrRawTextBreakout` hazard after `SetTagName` - nothing inserted, nothing renamed - and the shape that invites it is a sanitiser with an allowlist that unwraps everything not on it, since few allowlists include `noembed` or `xmp`. | upstream, gated | `settagname_test.go` `TestUnwrappingRawTextTurnsItsTextIntoMarkup` |
| B118 | A character the document's encoding cannot represent has three answers, not one. Content, attribute values, streamed content and a document-end insertion all emit a numeric reference - `&#128512;` for an emoji in windows-1252. `SetTagName` and `Comment.SetText` refuse it, because there is no reference in a tag name and a comment holds characters rather than references. `WithEncoding` said only that a reference is emitted. Measured for windows-1252 and iso-8859-2. | upstream, gated | `encoding_test.go` `TestUnrepresentableCharacterByPosition` |
| B119 | An `Option` holds no state and can be reused, and the function inside it is shared with every `Writer` it is given to - so building one option set at startup and reusing it per request shares every closure. Measured: four goroutines over a shared counting handler reported 655 of 800 matches, with two data races. "A `Writer` is not safe for concurrent use, but independent `Writer`s are fine" is a statement about the `Writer`. | binding, gated | `sharedhandler_test.go` |
| B120 | Ten element names hold content an HTML parser does not read as markup: script, style, iframe, noembed, noframes, noscript, xmp, textarea, title, plaintext. Nine can be closed from inside and insertions into those are checked; the two ways in that are not checked - a rename and an unwrap - are the caller's, and `IsRawText` is how a caller asks which names they are without copying the list out of a doc comment. | parser, gated | `rawtext_test.go` |
| B121 | Unwrapping or removing an element whose end tag is implied removes the token that closed it, which belongs to an enclosing element: `RemoveAndKeepContent` on the `<em>` in `<h1>a <em>b</h1>` removes the `</h1>`, so everything after the heading is inside it. The end tag callback reports `Name() == "h1"`, which is the only warning. The library documents where a foreign end tag *is*; it does not say that removing the element removes it. | binding, gated | `examples/gip/widows/main_test.go` |
| B122 | Everything that acts on an element acts on the token that closed it, and where the source omitted the end tag that token belongs to an enclosing element. `Remove` and `Replace` take the content up to it (documented); `RemoveAndKeepContent` takes the token alone, so the enclosing element never closes; `SetTagName` writes the new closing tag over it. `<h1><span>a <em>b</span> c</h1>` unwrapping the em loses `</span>`, an element the selector never named. | binding, gated | `removeimplied_test.go` |
| B123 | A comment token's delimiters are recoverable without the input: comment text is raw source bytes (a CR, a CRLF and a NUL all pass through unrewritten), so `SourceLocation().End - Start - len(Text())` is exactly the delimiter length. 7 is `<!--...-->`, 8 is `--!>`, 3 is a bogus comment or a CDATA section, 2 a processing instruction. The package documentation had said a stripper working from a stream could not tell them apart. | binding, gated | `commentshapes_test.go` |
| B124 | A sink write reaches the destination as it is made, so a `StreamFunc` that returns an error after writing has already committed what it wrote - unlike a handler that fails, whose insertions go with the rewrite. The committed prefix need not be well formed: a fragment that opened a `<div>` leaves it open. The first byte written to a sink is the commitment. | binding, gated | `streamcommit_test.go` |
| B125 | `SetAttribute` lower-cases the name of an attribute it is adding and keeps the document's spelling when updating one that is there, so an SVG `viewBox` can be changed and cannot be introduced - `<svg>` plus `SetAttribute("viewBox", ...)` gives `viewbox`, which a browser ignores. The read side keeps the spelling and offers it as `Attribute.NamePreserveCase`. `SetTagName` does not lower-case, and requires an ASCII letter first. | binding, gated | `attrnamecase_test.go` |
| B126 | Every write path refuses content or a name that is not valid UTF-8 - the `ContentType` insertions, `SetAttribute` name and value, `SetTagName`, `Comment.SetText`, `Sink.WriteString`, `Sink.WriteChunk` - and that fails the rewrite rather than the insertion. The document path refuses nothing: the same bytes pass through untouched, or become U+FFFD if a text handler is registered. So a rewrite can carry bytes it cannot write. `ErrInvalidUTF8` matches it, classified by checking the argument after a failure rather than by reading lol-html's wording. | binding, gated | `invalidutf8_test.go` |
| B127 | Text chunk boundaries do not only follow the writes: the tokenizer splits a text node at a `<` that does not begin a tag and delivers that character as a chunk of its own, so `<p>3 < 4 and 5 < 6</p>` is six chunks from one write. A `&lt;`, a `&`, a NUL and a CRLF split nothing; `<!`, `</` and `<?` end the text node, each beginning a comment token. Text node length alone never splits: 1 MB in one write is two chunks. | parser, gated | `textchunks_test.go` |
| B128 | `Element.IsRemoved` answers for an ancestor: an element inside one removed by `Remove` or `Replace` reports true (not under `RemoveAndKeepContent`), so an element handler needs no depth counter to know its content is on its way out. `TextChunk.IsRemoved` and `Comment.IsRemoved` do not - they report only that unit's own removal - so a text handler that accumulates has to be told by an element handler. Every insertion made inside a removed subtree is discarded, including from a grandchild and from a text handler, so the documented "insert first, remove last" hazard is about one element's own handlers. | binding, gated | `removedsubtree_test.go` |
| B129 | Case folding is ASCII everywhere it happens. `TagName` ASCII-lowercases, so `<DÉTAIL>` reports `dÉtail`; a selector is folded the same way, so a non-ASCII letter must match in case - `dÉtail` and `DÉTAIL` match it and `détail` matches nothing, with no warning. The `i` flag folds ASCII only, so `[data-x="é" i]` does not match `data-x="É"`. A tag name must also *begin* with an ASCII letter: `<ÉTAT>` is text. | parser, gated | `asciicase_test.go` |
| B130 | Source is not only undecoded, it is unpreprocessed. Measured against x/net/html: a CR or CRLF is a LF to a parser (in text, comments and attribute values) and is reported here as written; a NUL in element content is dropped by a parser, one in raw text or a comment becomes U+FFFD, one in an attribute value is kept - all reported as written; and one leading LF in `pre`, `listing` or `textarea` is dropped by a parser. So a comparison against a value from a DOM can fail on bytes neither side chose, and a CR written into an attribute reads back as a LF (`&#13;` survives). | parser, gated | `differential/preprocess_test.go` |
| B131 | A source range indexes the bytes fed to the rewriter, before decoding or transcoding: under `WithEncoding` a chunk reporting `café` has a four-byte range, so slicing the input works and measuring the reported string does not. An element's range is its start tag only; an end tag's is the token that closed it, shared by every element that token closed; a comment's and a doctype's cover the whole token. A range can be empty - the final chunk of a text node stands at the point where the node ended, which is how to find the node's extent, and a replacement character produced for an undecodable byte can stand at a point too. | binding, gated | `sourcelocation_test.go` |
| B132 | A narrow selector is much cheaper than a broad one that decides inside the handler, which is the opposite of what the cost documentation used to advise. Measured over a 2000-element page: three narrow selectors 439 allocations, one selector list 424, one `*` handler with a switch 4,228; and with nothing matching, fifty narrow selectors cost 351 against 4,031 for one `*`. A selector that does not match costs matching; a handler that runs costs a unit wrapper and whatever it reads. | binding, gated | `alloc_test.go`, `bench_test.go` |
| B133 | A nil `Option` in `NewWriter`'s variadic list was a nil pointer dereference inside the library, where a nil destination has always been a returned error. It is now `ErrNilOption`, naming the position, and refused rather than skipped: a rewrite that quietly did less than it was told to is worse than one that did not start. | binding, gated | `niloption_test.go` |
| B134 | Registering a text handler changes the document even if the handler does nothing: the text path decodes and re-encodes, so `<p>caf\xe9</p>` comes out as `<p>caf\uFFFD</p>`. The other paths do not - a comment handler leaves a comment's bytes alone and an element handler leaves an attribute's alone, on the same document. So "a read-only handler cannot change the output" holds for every kind but text, which matters for instrumentation added to a rewrite that has to be byte-exact. | binding, gated | `readonlytext_test.go`, `properties/properties_test.go` |
| B135 | Of the WHATWG Encoding Standard's 40 encodings, `WithEncoding` accepts 36. Three are refused as not ASCII-compatible - `utf-16le`, `utf-16be` and `iso-2022-jp`, whose bytes look ASCII until an escape sequence switches the charset - and `replacement` is refused as unknown. Labels are matched the standard's way: whitespace is stripped so a Content-Type value can be passed straight in, and nothing else is normalised, so `utf_8` is unknown while `iso88591` is windows-1252. | binding, gated | `encoding_test.go` |
| B136 | An attribute selector with an empty operand does not match nothing, which is what CSS says it must. Measured: `[a^=""]` and `[a$=""]` match every non-empty value, `[a~=""]` matches every value with no words in it, while `[a*=""]` matches nothing and `[a=""]`/`[a\|=""]` behave as specified. So a selector built by interpolating a prefix that turns out to be empty matches every element with that attribute instead of none. The `i` and `s` flags make no difference; an operand omitted altogether is a `SelectorError`. | parser, gated | `emptyoperand_test.go` |
| B137 | A class or id that starts with a digit cannot be written after `#` or `.` - a CSS identifier does not begin with one - and lol-html reports it as "The selector is empty", which describes what its parser had left. The escape is a hex one with a terminating space: `#\31 a` matches `id="1a"` and `#\31a` is U+031A and matches nothing. `SelectorError` now carries both that and the attribute-selector form, which needs no escaping. | binding, gated | `digitident_test.go` |
| B138 | A selector list is one selector: a handler runs once for an element however many parts of the list match it. Separate handlers are separate and each runs, including two registrations of the same selector. Measured on `<a href="/x" class="t">` with a handler that appends to an attribute: `a[href], a.t` gives one call and `data-n="x"`, while two handlers give two calls and `data-n="xx"`. So merging rules into one list is how to say "at most once per element", and it is also the cheapest form. | binding, gated | `selectorlist_test.go` |
| B139 | Strict mode's ambiguous-tag set differs by context, and the frameset list is not the select list minus one name. Measured over every element name in the HTML index: inside `<select>`, eight names (title style iframe xmp plaintext noembed noframes noscript); inside `<frameset>`, nine - those minus `noframes`, plus `script` and `textarea`, both of which are fine in a `<select>`. And `select`, `textarea`, `input` and `keygen` end the ambiguous context in a `<select>` and end nothing in a `<frameset>`, where only `noframes` does. | parser, gated | `strict_test.go` |
| B140 | A `Doctype` can be read and removed and never written: the type has no insertion methods, so upgrading a legacy declaration means removing it and inserting one before the first element. Measured against x/net/html, that works for a document whose first thing after the doctype is an element, a comment or whitespace, and silently produces a quirks-mode document for one with text before its first element, one with no elements, or one that is only text. Adding a second doctype instead is no help: a parser drops it and the legacy one still applies. | binding, gated | `differential/doctype_test.go` |
| B141 | Foster parenting cuts both ways: content inserted into an element can land outside it in the tree. Measured against x/net/html, prepending a hidden input to a form gives `form > input` where the form is an ordinary child or a table cell's child, and `table > input`, `tbody > input` or `body > input` where the form sits between a table and its rows or inside a select. The bytes say the field is in the form and the tree says it is beside it, so "the markup looks right" is not the test for an insertion whose position is the point. | parser, gated | `differential/table_test.go` |
| B142 | An `OnEndTag` registration costs a live handle until the rewrite ends, not until the end tag fires: the count climbs through five closed siblings and only falls when the Writer closes. So the cost is per matched element rather than per open element, about 300 bytes each - 100,000 sibling divs give 100,001 handles and about 30 MB of Go allocation against 6 MB without the registration - and `MaxMemory` does not bound it, because that limit is lol-html's parsing buffer and this is the binding's handle table. | binding, gated | `endtagcost_test.go` |
| B143 | A graceful bail-out serves *unrewritten* input, and its rewritten prefix can be empty. Measured on 201 paragraphs in 64-byte writes with a handler setting an attribute on each: at `MaxMemory` 560 the default served 0 bytes and graceful served 64 bytes verbatim with nothing rewritten, while 900 completed with all 201 rewritten. The memory error is returned in both modes, so the choice is only about what the client receives: for a rewrite that adds something, the input is the better failure; for one that removes or neutralises something, serving it is serving what the rewrite existed to stop. | binding, gated | `gracefulbailout_test.go` |
| B144 | A `<template>` is markup that is not on the page, and its content parses by its own rules. Handlers fire inside it at any depth and a descendant selector crosses the boundary, yet the content is inert until a script clones it - so a count that mixes template media with page media counts nothing in particular. Measured against x/net/html: `<template><tr><td>x` keeps the row where `<div><tr><td>x` drops both tags, a template inside a table is not fostered out where a div is, and prepending an *element* into a template holding rows deletes the rows (a comment, text, `Append` and `Before` do not). The table trade runs the other way: there the insertion moves and the content survives. | parser, gated | `differential/template_test.go` |
| B145 | A descendant selector keeps matching after the ancestor has ended. The selector engine pops its stack on end tag tokens and a start tag never pops anything, so for every element whose end tag a document may omit, `ancestor descendant` matches everything after it until an explicit end tag arrives. Measured against x/net/html over a second list item, paragraph, table cell, row, definition and option: `<ul><li><video><li><track></ul>` fires `video track` where the tree puts the track in the second item with no video above it, and the over-match reaches any depth and stops at the enclosing `</ul>`. The child combinator cannot be fooled this way, because the start tag that ended the element is the parent of what comes next. | upstream, gated | `differential/impliedclose_test.go` |
| B146 | A wrapper is two insertions and the parser decides whether they wrap. Measured against x/net/html: a `<div>` wrapper around an element inside a `<p>` takes it out of the paragraph, leaves the following text outside too and turns the source's `</p>` into a second empty paragraph, while a `<span>` wrapper wraps - and a span cannot hold an element that closes a paragraph by starting, coming out empty with the element outside it. So the question is whether the wrapped element closes a paragraph, not whether the wrapper is block or inline, and for a `<table>` the answer depends on the doctype: outside quirks mode the div is right and inside it the span is. A wrapper around a table-internal element is fostered out and wraps nothing. | parser, gated | `differential/wrap_test.go` |
| B147 | A value is only source for the context it came from, and `EscapeText`'s advice that "text can usually be written through raw" is the unsafe half. Each context lets through the character the other ends on: an attribute may hold a raw `<` and text may hold a raw quote. Measured against x/net/html by counting the tree: a `title="<img src=x onerror=alert(1)>"` moved into an element's text raw becomes an img element with a working onerror, and a heading's text containing `a" onload=alert(1) x="b` moved into an attribute raw gives the new element an onload. Escaping the destination's terminator alone is enough and is what `Text` and `SetAttribute` apply; `EscapeText`/`EscapeAttribute` on a document value escape its `&` a second time. | binding, gated | `differential/context_test.go` |
| B148 | An HTML tag name inside an `<svg>` ends the svg, and the library's two views of that disagree. Measured against x/net/html: 44 names break out of foreign content - b, big, blockquote, body, br, center, code, dd, div, dl, dt, em, embed, h1-h6, head, hr, i, img, li, listing, menu, meta, nobr, ol, p, pre, ruby, s, small, span, strong, strike, sub, sup, table, tt, u, ul, var - plus `font` when it carries a color, face or size attribute, and everything after the offending tag is document content. `NamespaceURI` follows the break-out and reports HTML; the selector engine does not, so `svg > circle` matches a circle the tree puts outside the svg. Neither answers "is this still inside the image", so a rewrite inlining SVG has to look for the names itself. | parser, gated | `differential/foreign_test.go` |
| B149 | `MaxMemory` is bounded by the largest single token a handler is given, and only where that token straddles two writes - not by the document. Measured on a 2012-byte `<img>` tag and on 2800 bytes of 14-byte tags: one Write needs 5 bytes whatever matches; 64-byte writes need 2012 when a handler matches the long tag and 68 when nothing does; 200 short tags need 76. Text costs nothing because it arrives in chunks, a comment costs its whole length because it arrives whole, and what the rewrite writes does not count (growing an attribute 64 times does not move the floor). So adding a handler can raise the limit a pipeline needed before, with no change to the document. | binding, gated | `memoryfloor_test.go` |
| B150 | Handlers on one element share the element, and the package documentation said the opposite ("a rewrite cannot act on what another handler produced"). A later handler reads an earlier one's `SetAttribute` value and its `SetTagName` name, so two selectors that both match and both write give `<img src="/a.js?v=1?v=2">`, and swapping the registrations swaps the result: the firing is order-independent and the outcome is not. What a later handler cannot do is *match* on the edit - an added class does not fire a `.new` selector and a renamed tag does not fire a handler on the new name. Also measured: removing an attribute and setting it again moves it to the end of the tag, while setting one and removing it leaves the tag byte-identical. | binding, gated | `handlerstate_test.go` |
| B151 | Structural selectors count tokens rather than children, so an omitted end tag turns siblings into descendants. Measured against x/net/html on `<ul><li>a<li>b<li>c</ul>`, whose tree has three items in one list: `ul > li` matches 1, `li > li` matches 2, `li:first-child` matches all three, and `li:nth-child(2)`, `:nth-child(3)` and `:nth-of-type(2)` match none - against 3, 0, 1, 1 with `</li>` spelled. It is not an off-by-one: in `<ul><li><img><li><img></ul>` the second item is `li:nth-child(2)` as the second child of the first item. Paragraphs, table cells, table rows and definition lists behave the same way, and a document that closes some items and not others is partly right. | upstream, gated | `differential/structural_test.go` |
| B152 | Rewriting the text of a raw-text element inverts both rules that govern inserting into one. `TextChunk.Replace` with `Text` escapes the three markup characters, which raw text does not decode - so a stylesheet's `.a > .b` comes back as `.a &gt; .b` and a script's `a < b && c > d` as `a &lt; b &amp;&amp; c &gt; d`, and a `<title>`'s `&amp;` is escaped twice. `HTML` is therefore the content type for editing a script body or a stylesheet. And the breakout guard does not cover it: an `Element` method refuses a `</script>` in inserted content, while `TextChunk.Before`, `.After` and `.Replace` write it out, because lol-html hands a chunk over with no way to ask what element it came from. Fixed by exporting the check as `CheckRawText`, which is what the element paths apply. | binding, gated | `rawtextrewrite_test.go` |
| B153 | Registration cost is per handler, not per selector clause: a selector list is one selector. Measured over 500 elements that match nothing, so nothing but registration and matching is counted - no handlers 16 allocations, one selector 24, a twelve-clause list 24, twelve separate registrations 96. So naming twelve elements in one `OnElement` costs what naming one costs while twelve `OnElement` calls cost eight times as much. The list is slower per element than a single clause and faster than twelve registrations, and it allocates nothing extra. | binding, gated | `reportshape_test.go` |
| B154 | Failing a rewrite from a handler is not atomic. Everything before the token whose handler failed has already reached the destination - measured identical at one Write and at 64-, 16-, 4- and 1-byte writes - and the prefix is a whole number of tokens, so it is well-formed markup a client reads as a short page. A handler that fails on the document's first element delivers nothing; one that fails in `OnDocumentEnd` has already delivered everything and the error only surfaces from `Close`. So refusing a document means the caller buffers and forwards on success. | binding, gated | `handlerfailure_test.go` |
| B155 | `<image>` is a spelling of `<img>`: the parser renames it and carries the attributes over, so a browser fetches the file and runs its `onerror`, while the rewriter reports what the document spelled - `TagName() == "image"`, and a selector for `img` matches nothing. Measured against x/net/html over five spellings including a self-closing one and an explicit end tag. Every rewrite keyed on `img` therefore has a hole: match `img,image` and check `NamespaceURI`, because SVG's own `image` keeps its name. Nothing else on the obsolete list is renamed - center, font, marquee, acronym, applet, keygen, isindex and eleven more all reach the tree under their own names. | parser, gated | `differential/imagealias_test.go` |
| B156 | A rename changes how the element's existing content is parsed. `SetTagName` writes over the tag and leaves the content alone - it was tokenised under the old name - while whoever reads the output applies the new name's content model to it. Measured against x/net/html: `<div><p>x</p></div>` renamed to `table` fosters the paragraph out of the element, and renamed to `select` deletes the paragraph and a span beside it and merges their text, with no error and the output being exactly the markup asked for. So a rename is safe when the new element accepts what the old one held, which is the condition `examples/gip/modernise` renames within. | parser, gated | `differential/rename_test.go` |
| B157 | `html.UnescapeString` is the parser's decoder for text and not for an attribute value, where a named reference without its semicolon is not a reference when the next character is `=` or ASCII alphanumeric. Measured against x/net/html: `?a=1&copy=2`, `?a=1&notit=2`, `?a=1&amp=2`, `?a=1&lt=2` and `?a=1&noti` all reach a browser unchanged and all decode in the standard library, while the same names with semicolons, and any of them at the end of a value or before a non-alphanumeric, agree. In text the two agree everywhere. The library's own advice - decide on the decoded form, rewrite the raw one - therefore pointed at a decoder that is wrong for attributes, which is where URLs live. | binding, gated | `differential/attrrefs_test.go` |
| B158 | Invalid bytes are lossy to read for every unit kind and lossy to write for text alone. A handler is handed U+FFFD and never the byte, so no rewrite can see what the document held - and with a text handler registered, invalid bytes in text or raw text come back as U+FFFD in the *output*, while an attribute value, a comment and a tag name read the same way keep their bytes, being re-emitted from the source. So a tool that reads text to diagnose a mis-declared document is the pass that damages it, and "this document is not UTF-8" can only be reported as "the text contains U+FFFD". | binding, gated | `invalidutf8_test.go` |
| B159 | A rewrite cannot convert a document's encoding: the output is in the document's encoding throughout, there is no output-encoding option, and replacing every text chunk and attribute with itself leaves the bytes identical - measured over windows-1252, iso-8859-2, shift_jis, euc-jp and gbk, all of which also round-trip byte-for-byte through a text handler. And a byte the decoder cannot use has two shapes: declared UTF-8 it reaches the output as the bytes of U+FFFD, and in a legacy encoding as `&#65533;`, seven bytes where the document had one, because U+FFFD is outside the target repertoire. What the rewriter can do for a conversion is prove it: reading both versions and comparing what the handlers were given. | binding, gated | `legacyencoding_test.go` |
| B160 | The declared encoding changes what a document says and never what counts as markup. Measured over all 36 accepted encodings against a corpus that puts every markup character after nine different lead bytes: the byte spans of the elements, their names, their attribute names and the spans of the text and comments are identical in every one, and identical to `x-user-defined`, which cannot combine bytes even in principle - while the same three bytes read as at least ten different strings. So the encoding-based smuggling that legacy multi-byte encodings allow in a browser does not happen here, and a label from an untrusted header cannot move a tag boundary. Source locations are byte offsets whatever the encoding, which is what makes the comparison possible. | binding, gated | `encodingstructure_test.go` |
| B161 | Permissive mode is not silence, and the documentation said it was ("no handler runs for it"). Measured on `<select><xmp><script>alert(1)</script></xmp></select><p>after</p>` with strict off: the select, the xmp and the paragraph after the region all fire element handlers, the script does not because it is raw text, and the script's source arrives at a text handler in chunks split around the `<`. So the ambiguous element is an element, a closed ambiguous tag costs only its own content - an `<img>` after a `<title>` in a `<select>` still fires - and the missed markup is text a rewrite can refuse on. | binding, gated | `strict_test.go` |
| B162 | The memory floor is not a function of the write size, and `MaxMemory` does not bound the document. A megabyte of small paragraphs completes under a 1 KiB limit in one Write, while 10 KiB of the same paragraphs in 4 KiB writes does not - so the option is no defence against a large body, only against a large token. And the floor depends on where the boundaries fall relative to the tokens: measured on two 14 KB documents, one needed 4930 bytes at both 4095-byte and 4096-byte writes and the other needed 4928 at 4095 and 832 at 4096, so a larger write can want a smaller limit. Separately, the fixed part of the floor is "an element handler exists": no handler and a text handler both complete at 5 bytes in one Write where an element handler needs 832, whether it reads an attribute or sets one. | binding, gated | `memoryinput_test.go` |
| B163 | "Safe to call more than once" means a second `Close` does nothing and returns nil, including after a failure - so a caller whose only check is on a `Close` that runs second sees nil for a rewrite that failed. The first `Close` reports `ErrPoisoned` wrapped around the cause, as documented; the shape to avoid is an explicit `Close` in an error path together with a deferred one that assigns to the returned error. Deliberate and asserted in `faults_test.go`; now stated on `Close` and demonstrated by `examples/gip/poisoned`, whose table also pins what each kind of failure leaves at the destination. | binding, gated | `faults_test.go` `examples/gip/poisoned` |
| B164 | A retained [Sink] is the seventh unit with a handler-bounded lifetime, and the `ErrDetached` list named only six. It is also the best-behaved: `WriteString`, `WriteChunk`, `AsWriter().Write` and `Err` all report `ErrDetached`, so a retained sink cannot be mistaken for a working one - where a retained element's getters answer as if the document were empty, and `Attribute` reporting `("", false)` is indistinguishable from an absent attribute. | binding, gated | `detached_test.go` |
| B165 | Two handlers can run inside `Close`, not one: `OnDocumentEnd` always, and a text handler for the last chunk of a text node the document left open - measured, a closed element delivers every chunk during `Write` while `<p>a` delivers its boundary chunk during `Close`. So a text handler's error or panic can surface from `Close`. An end-tag handler for an element nothing closes never runs. And a panic from a handler inside `Close` leaves the Writer *closed* rather than poisoned - a later `Write` reports `ErrClosed` - while a panic from `Write` poisons it with the bare sentinel. | binding, gated | `examples/gip/panics` |
| B166 | `SetUserData` costs a live cgo handle per unit, held until the Writer closes - the same cost class as B142 and not bounded by `MaxMemory`. Setting it to nil releases the handle immediately, and replacing a value releases the one it replaced. For text chunks the unit is the chunk, so the cost depends on the caller's write sizes: one 2000-byte text node holds 2 handles written whole and 2001 written a byte at a time. `ClearEndTagHandlers` stops the callbacks and keeps the handle. | binding, gated | `userdatacost_test.go`, `examples/gip/unbounded` |
| B167 | A handler error leaves the sink holding a rewrite of a prefix of the input - byte for byte what a fresh rewriter produces from that many bytes - rather than a truncation, and the unit whose handler stopped is not emitted. Where the prefix ends is a fact about the document for element, end-tag and comment handlers, and a fact about the write sizes for text handlers, since a chunk is not a position. The caller's sentinel survives in `Write`, in every refused later `Write`, and in `Close` under `ErrPoisoned`. | binding, gated | `earlystop_test.go`, `examples/gip/stopwhen` |
| B168 | A destination-writer failure stops the rewrite completely: no further element, comment or text handler runs, and `OnDocumentEnd` never runs at all, so accounting written there is lost on exactly the runs a caller cares about. Where it stops does not depend on the write sizes, and a destination that accepts nothing still sees one handler run. `Close` discovers a destination that broke after the last `Write` only when `Close` is the call that writes - a document ending inside a token, or a handler appending at the document end. | binding, gated | `sinkfailure_test.go`, `examples/gip/clientgone` |
| B169 | The number of writes the destination receives is decided by matching rather than by editing: a handler that does nothing costs two writes per matched element, where the same document with no handlers is one write. An end-tag handler costs three, `RemoveAttribute` six and `SetAttribute` thirteen, mostly of one byte. A selector that matches nothing costs nothing. Output bytes are identical in every case, so a read-only instrumentation pass changes the write pattern and not the document. A `bufio.Writer` collapses all of it to buffer-fulls. | upstream, gated | `sinkwrites_test.go`, `examples/gip/backpressure` |
| B170 | A `Writer` is an `io.Writer`, so rewriters compose into a streaming pipeline: the downstream stage sees bytes before the upstream stage has the whole document, and neither holds it - peak heap flat at about 3 MB piping 1, 4 and 16 MB. Allocations are the same doubling as a buffered second pass (1645 piped against 1655 buffered, over 400 anchors) and the output is identical. Close upstream first: the wrong order truncates the tail and reports `ErrClosed` from the upstream `Close`. An error in any stage reaches the caller through the stages above it with its identity intact. | binding, gated | `pipeline_test.go`, `examples/gip/pipeline` |
| B171 | Touching the attribute list or the tag name re-serialises the whole start tag: `SetAttribute` (even to the value already there), `RemoveAttribute` of a present attribute, and `SetTagName`. Each attribute's own source text survives - quoting style, name case, spaces around the equals, entities, duplicates, bare booleans - while the separators between attributes are regenerated as single spaces, a trailing space before the bracket is dropped and a missing one added. Reading, inserting, end-tag handlers and user data leave the bytes alone. Setting one attribute on every anchor of cloudflare.com.html takes it from 119,237 to 114,542 bytes. | upstream, gated | `reserialise_test.go`, `examples/gip/twoways` |
| B172 | A `Writer` cannot be reused, so a workload of many small documents pays the whole selector registration per document: 23 allocations for one selector, 105 for ten, 406 for fifty, and the document adds almost nothing where nothing matches. On an M3 Pro fifty selectors is about 38µs of construction, more than rewriting a 1 KB document with them costs. Worker counts for such a queue peak below the core count - 400 items took 39ms on one worker, 12.1ms on four and 20.2ms on eight, on twelve threads. | binding, gated | `fixedcost_test.go`, `examples/gip/queue` |
| B173 | Registering a text handler makes a rewrite lossy for any body that is not valid in the declared encoding, and silently: a gzip body comes back longer and no longer decodable, a PNG header two bytes longer, 256 arbitrary bytes 482 bytes. With element handlers only, every one of them is byte-identical - so the same mistake either destroys a compressed response or rewrites nothing, depending on the handler set. `NewWriter` refuses `utf-16le` and `utf-16be` with an `EncodingError` for not being ASCII-compatible, which is how a proxy can decide by trying. | upstream, gated | `lossytext_test.go`, `examples/gip/proxy` |
| B174 | Whether a wrapper escapes an open `<p>` depends on the wrapper's tag and, for `<table>` alone, on the document's mode: a table start tag closes an open paragraph in standards mode and not in quirks mode, so a table wrapper inserted inside a paragraph lands beside it with `<!doctype html>` and inside it with no doctype or a legacy one. `<div>`, `<section>` and `<ul>` leave the paragraph in both modes; `<span>` stays in both. | upstream, gated | `differential/tablewrap_test.go`, `examples/gip/tablelayout` |
| B175 | A combinator inside `:not()` is rejected, and only inside it: `:not(div p)`, `:not(div > p)`, `:not(div + p)` and `:not(div ~ p)` all fail while `div p` and `div > p` work outside and `:not(div.a)`, `:not(div, span)`, `:not(:nth-child(2))` and `:not(:not(div))` are accepted. The documented rule - supported if the rewriter can decide it at the start tag - predicts `:not(div p)` works, since the plain descendant selector decides exactly that question there. So there is no selector for "not inside an X" and a handler has to keep a stack. The error names the pseudo-class rather than the combinator inside it. | upstream, gated | `selector_test.go` `TestACombinatorInsideNotIsRejected`, `examples/gip/islands` |
| B177 | A tag the document never finishes is the one construct that reaches no handler at all, and its bytes are still emitted: `<p`, `<p attr="v`, `</p` and `<script` produce no element, text, comment or doctype call, while `<!-`, `<!--`, `<!`, `<?php`, `<![CDATA[x` each produce a comment with the text so far, `<!DOCTYPE` a doctype, and `<script>var a` the element and its text. A stray end tag is unreported too and harmless. Which constructs *swallow* what follows is a wider set and the one B39 already lists - tag, comment, doctype and any open raw-text element, but not an element merely left open. The consequence for composition: rewriting fragments with separate rewriters and joining the outputs is not the same as rewriting the whole. On `<p>a</p><script>alert(1)</script><p>b</p>` with every script removed, 7 of the 40 cut points - exactly those strictly inside `<script>` - reassemble a live script element neither pass saw, and the cut immediately after the start tag leaves the payload as text beside a stray end tag. One rewriter is correct at every chunk boundary. A fragment can be tested rather than trusted by appending a sentinel element and seeing whether its handler runs; a string scan for `<` after the last `>` calls 1007 of 4000 generated fragments safe when they are not, and never over-reports - it cannot know that `<!DOCTYPE` does not start with a letter, that an open `<script>` has its last `>` behind it, or that a bare `</` is an unfinished end tag. | upstream, gated | `fragment_test.go`, `examples/gip/inventory` |
| B178 | Renaming a container to a name that cannot hold content has four outcomes, and none of them is the element with its content. Measured against x/net/html on `<div class="w">x</div>`: `br` gives **two** elements, because `</br>` is the one end tag HTML treats as a start tag; `img`, `hr`, `input`, `wbr` and `area` give one element with the content as its sibling; `col` gives **none**, since a col outside a table is dropped; and `meta` puts the element in `<head>` and leaves the content in `<body>`. The output markup is identical in shape in all eight cases and nothing errors. A hyphenated name is always an ordinary container, which is what makes a custom-element target safe. | upstream, gated | `differential/rename_test.go` `TestARenameIntoAVoidNameHasFourAnswers`, `examples/gip/widgets` |
| B179 | The lower-casing of attribute names breaks any consumer that is case-sensitive, not only SVG. A framework template is HTML-shaped text whose attribute names are identifiers to a compiler: `*ngIf` reads as `*ngif` from `Attribute.Name`, `[ngClass]` as `[ngclass]`, `v-bind:someProp` as `v-bind:someprop`, and `Attribute.NamePreserveCase` gives the spelling back. `SetAttribute("*ngIf", …)` on an element that does not have it writes `*ngif`, which is not a directive; updating one already there keeps the spelling, and `Replace` with built markup is the other way. The documentation said "in HTML that is nothing", which is true of an HTML parser and not of the next reader. | upstream, gated | `examples/gip/bindings`, `attrnamecase_test.go` |
| B180 | Without `WithESITags` there is exactly one lossless way to expand an `<esi:include>`, and it leaves the marker in the output. On `<div><p>before</p><esi:include src="/f"/><p>after</p></div>`: `Replace` and `Before`+`Remove` lose `<p>after</p>` and `</div>`, `SetInnerContent` keeps the marker and loses `<p>after</p>`, `RemoveAndKeepContent` loses `</div>`, and `Before` alone is correct. The source tree already nests what follows the include *inside* it - measured against x/net/html, and `div > p` matches one paragraph without the option and two with it - so `Before` preserves the source tree exactly. The trap is `Before` plus `RemoveAndKeepContent`: it gives the option's tree on a document that ends soon after the include and re-parents everything on one that does not, because the consumed `</div>` makes a following `<section>` a child of the div instead of its sibling. | upstream, gated | `differential/esi_test.go`, `examples/gip/esi` |
| B181 | A comment appended at the document end is swallowed by the same truncated inputs as an element - mid script, style, textarea, title, doctype, start tag, end tag or tag name - with one row of its own: a document ending inside a *comment* absorbs the appended comment into itself, so the output holds one comment carrying both the page's text and the marker, including the marker's own `<!--`. Counting comments therefore says the marker arrived, and searching for its text says so too; only the surrounding text shows otherwise. B39 measured this family in elements, where the mid-comment case simply loses the element. | upstream, gated | `examples/gip/servertiming` |
| B182 | A comment between the doctype and the first element does not stop the doctype counting: measured against x/net/html, one comment before it, two comments before it, a comment after it and whitespace before it are all standards mode, while no doctype and a legacy doctype are quirks. So the top of the document is a safe place for a stamp. There is no *guaranteed* place, though: the tree always holds `html`, `head` and `body` while the source usually does not, and a rewriter reports the source - `<!doctype html><p>x</p>` reports one element - so a selector on `head` matches nothing on most fragments, and an anchor has to be the first element of any kind, or the document end when there are none. | parser, gated | `differential/buildinfo_test.go`, `examples/gip/buildinfo` |
| B183 | A `<meta>` inserted before the first element lands in the head, because a parser in its "before head" mode meets the meta, creates the head and puts it there - so a rewrite does not have to build the head, and a `<head>` of its own gives an identical tree (and is dropped as a duplicate where the source has one). Measured against x/net/html for a whole page, a page with no head spelled, `<html>` with no head, `<body>` first, `<title>` first, a comment first, whitespace first and no doctype. It lands in the *body* wherever text got there first, since text ends the head: `text`, `&amp;`, and - the trap - anything that looks like whitespace and is not. Only tab, LF, FF, CR and space keep the head open; U+00A0, U+2007, U+202F, U+3000 and the vertical tab do not, so a template that indents with a non-breaking space moves every meta it adds into the body. The condition is detectable in one pass, because text arrives before the element that follows it. | parser, gated | `differential/deployid_test.go`, `examples/gip/deployid` |
| B184 | A pass with no handlers registered costs about **8%** of a pass with them: measured over a 347 KB page, 406µs against 5.085ms for the same document rewritten, because with nothing registered the sink hands the destination a slice over lol-html's own buffer rather than copying. A collect-only pass (handlers, no output) is 0.66x. So two passes that collect and then copy cost 0.75x of one pass that rewrites - less than one pass - while holding nothing; two passes that collect and then rewrite cost 1.67x against 1.44x for one buffered pass, and buffering holds the whole document (+72 KB, +351 KB and +1.76 MB of live heap for pages of 68 KB, 348 KB and 1.4 MB) where two passes hold nothing at any size. | binding, measured | `examples/gip/cachetags` |
| B185 | What the rewriter holds before emitting is one start tag, and which tags depends on the *selectors* rather than on whether a handler ran. Measured as the widest gap between input and output over a 5513-byte `<div>` with five hundred attributes, fed a byte at a time: no handlers 5 bytes, `a[href]` 5, `span[data-x]` 5, `p` 5 - a tag name rules a selector out at the name and the rest streams. `div[data-x]`, `div[data-absent]`, `div.absent`, `div#absent`, `[data-absent]`, `div` and `*` all hold 5505, matching or not, because an attribute, class or id cannot be ruled out until the tag ends and a selector with no tag-name component excludes nothing. Text is never held: a 10 KB text node ran a gap of 3 bytes with a text handler and without one. This refines B78, which distinguished "a handler registered" from "none"; the line is the tag name. | binding, gated | `examples/gip/tee` |
| B186 | A rewriter writes during its own `Close` - `OnDocumentEnd` always, and a text handler for the last chunk of a text node the document left open (B165) - so in a chain with a compressing writer the order of the two `Close` calls decides whether those bytes arrive. Measured: closing the gzip writer first loses 37 bytes of an appended comment, and 4 bytes of held text where the document ends inside the text node, while a complete document with no append is unaffected either way. The rewriter's `Close` returns `flate: closed writer`, so it is detectable - and `defer` is where the report dies, since deferred calls run last-in-first-out, so the defer written *second* runs first. Separately the write pattern costs nothing: the same document at chunk sizes 1, 16, 512 and 1 MiB gzipped to 1188 bytes each, identical to compressing the finished output in one write, so wrapping the compressor in a `bufio.Writer` buys nothing. | binding, gated | `examples/gip/gzipout` |
| B187 | Rewriting a gzipped body needs the size limit *after* the decompressor and needs it to speak. Measured: 50991 compressed bytes expanding to 52428800 reach the rewriter in full with no limit, are bounded at 1048576 by an `io.LimitReader` after the decompressor, and reach the rewriter in full with the same limit placed before it - the compressed input was never near it. And a plain `LimitReader` ends the stream at the limit, which downstream is indistinguishable from the document ending: the copy succeeds, `Close` succeeds, and the output is half a page with no error anywhere. Reading one byte past the limit is what separates a short document from a truncated one. Separately, a truncated gzip stream can still deliver every byte - cut at 90% the deflate data is complete and only the trailer is missing - so the trailer error is about integrity rather than completeness, and it arrives after the bytes have been written. | binding, gated | `examples/gip/gunzip` |
| B188 | Multipart is the join B177 warns about, made safe, because the delimiter is out of band: a boundary is written by the multipart writer rather than by the rewriter, so a part ending in `<div attr="`, `<div`, `</div`, `<!-- unfinished` or `<script>var x = 1` keeps its truncated tail and the next part is untouched. The same two fragments concatenated without boundaries lose the second one's paragraph. And a rewriter per part is free until the parts are small: over 100000 bytes total, one rewriter per part against one rewriter for all of it measured 0.98x at 1 part, 0.99x at 10, 1.02x at 100 and **1.54x at 1000 parts of 90 bytes**, where B172's fixed registration cost exceeds the content. Closing a part's rewriter after the next part has begun gives `multipart: can't write to finished part` - B186 in a second place, and lost to a deferred Close the same way. | binding, gated | `examples/gip/multipart` |
| B189 | Stopping a rewrite at a handler and copying the rest of the input from that element's `SourceLocation().Start` is exact and enormously cheaper than rewriting the whole document with the handlers gated off. The documented early-stop guarantee - what has reached the sink is what a fresh rewriter produces from that much input, with the stopping element not emitted - is what makes the offset the right place to resume. Measured on a 114-byte head, fastest of twenty runs: with a 200 KB body, stop-and-copy is 16µs against 1.842ms gated and 375µs for a no-handler pass over the whole document - 115x and 23x. The outputs are byte-identical. Where the head ends cannot be `body`, because a document need not spell one; the rule is the first element outside `base`, `link`, `meta`, `noscript`, `script`, `style`, `template`, `title`. | binding, gated | `examples/gip/headonly` |
| B190 | Resuming an input after an early stop takes a different offset for a text marker than for the others. The documented prefix rule gives the answer: for a comment, an element or an end tag the prefix ends *before* the unit, so the resume point is its `SourceLocation().Start`; for text the prefix ends before that *chunk*, and by the time a marker is recognised the node has been accumulated to `IsLastInTextNode`, so the prefix holds the whole node and the resume point is the last chunk's `End`. Measured: on `<p>before STOP here</p><p>after</p>` resuming at the node's start (3) emits the text twice and resuming at its end (19) is exact. Separately a marker inside a raw-text element is not a marker for a comment handler - measured for script, style, textarea and title - but *is* handed to a document-level text handler, so the text path needs a selector-associated handler on those four to exclude it. | binding, gated | `examples/gip/stopafter` |
| B191 | A document can be cut into regions rewritten by different handler sets, and the rule is "cut where nothing is open". Most of what a region sees is context-free, because a rewriter is a tokenizer: a `td` alone is the same token as a `td` in a table, where a tree builder would disagree. End tags are the exception - an end tag pairs with a start tag, so an element spanning a boundary is split and its `OnEndTag` handler runs in **neither** half (measured: `<div><p>A</p><p>B</p></div>` cut at 13 runs the div's end-tag handler once as a whole and zero times across the halves). So a boundary inside a table row, a select, a list or a div is unsafe for handlers that touch end tags and safe for handlers that do not. The sentinel test of B177 answers absorption, not cuttability: it misses a prefix ending in a bare `<` or `</`, which swallows nothing and orphans the tail's tag name, and it cannot see an open element at all - measured, 10 of 79 offsets pass it and are unsafe. The exact check is to rewrite both halves with a probe touching every unit kind and compare against the whole. | binding, gated | `examples/gip/regions` |
| B192 | `OnDoctype` fires for every doctype token in the source, and a parser honours only the first one with nothing but whitespace and comments before it. Measured against x/net/html by B174's behaviour: a doctype after text, after an element, or after a non-breaking space is reported as `html` by the handler and leaves the parser in **quirks** mode; only the ASCII whitespace and comment prefixes reach standards mode. So a rewrite that chooses standards-mode behaviour on the strength of the handler alone is wrong on those three, silently. A doctype can also be arbitrarily far in - after a 5000-byte comment it ends at byte 5022, after two hundred comments at byte 2015 - so a bounded peek to choose handlers before building the `Writer` is a limit on correctness for no gain: measured, peek and choose 3.002ms against 2.959ms for registering both sets and gating them. | parser, gated | `differential/doctypemode_test.go`, `examples/gip/doctypepick` |
| B193 | Registering selectors on one `Writer` is linear in allocations and superlinear in time. Measured on an M3 Pro, fastest of ten: 10 selectors 8µs, 100 81µs, 500 615µs, 1000 1.956ms, 2000 5.896ms, 4000 **23.702ms** - four times the selectors for twelve times the time - while allocations stay at about 5.4 per selector, flat from 500 up. B172 recorded the allocation figure; this is the other half. How superlinear is the machine's: on the musl runner the per-selector cost went 6872ns at 100 to 10063ns at 2000, a factor of 1.46 against 2.4 on the laptop, because the fixed cost there is nine times larger and dominates - so the shape is gateable only through the allocation count. One consequence: validating 1000 selectors one at a time took 1.55ms against 1.944ms for registering them together, so checking each separately is cheaper as well as more informative - and it has to be separate to be informative at all, because `NewWriter` returns on the first rejection and names exactly one bad selector however many the list has. | binding, gated | `examples/gip/selectorcheck` |
| B203 | A leading byte-order mark is neither sniffed nor stripped, and it is the one declaration that outranks the caller's. `WithEncoding` documents that a `<meta charset>` is never consulted, which agrees with a browser because a `<meta>` ranks **below** a transport charset; a BOM ranks **above** it. Measured against the sniffing algorithm in `golang.org/x/net/html/charset`: a UTF-8 BOM answers "utf-8, certain" whether the declared label is `windows-1252`, `shift_jis` or absent, and `\xff\xfe`/`\xfe\xff` answer utf-16le/utf-16be the same way. Here the label wins regardless, so `\xef\xbb\xbf<p>café</p>` declared `windows-1252` gives handlers `"ï»¿"` and `"cafÃ©"` where a browser reads `café` - silently, and byte-identical on output unless a text handler is registered. The mark is also reported **as text**: the first chunk is U+FEFF at 0..3, so anything accumulating text gets a character the page never shows, and a second mark is content rather than a mark. A UTF-16 document cannot be rewritten at all - its markup is not ASCII-compatible and a text handler turns the mark itself into U+FFFD - so refusing it is the honest answer. | binding (documentation), gated | `examples/gip/bom` |
| B200 | A comment built by hand had no guard, though `Comment.SetText` refuses the same text and `CheckRawText` is exported for exactly the equivalent hand-built case. The path is ordinary: `DocumentEnd.Append` and the insertion methods take markup, so a summary emitted as a trailing comment is assembled by the caller. Unguarded it leaks silently - ` rewritten: a=1; first: /x?q=a-->b ` comes back as the comment ` rewritten: a=1; first: /x?q=a` with the rest as page text. The rule, now `CheckComment`: refuse text containing `-->` or `--!>`, or beginning with `>` or `->`. Nothing else is special - `--`, `--!`, `----`, `<!--`, a trailing `-`, a NUL, `]]>` and a `>` after the first character all round-trip. Agreement with `SetText` is pinned over 2813 strings (every string up to 4 characters of `- ! > < a`, newline and space), 474 of them refused by both, and the leak is checked independently by parsing the output. One subtlety the app's own test caught: the danger needs a **literal** `-->` in the source, since `Attribute` returns raw source and `&gt;` stays encoded - a literal `>` being legal inside a quoted attribute value. | binding, gated | `commentcheck_test.go`, `examples/gip/tailcomment` |
| B195 | A text chunk's `SourceLocation` moves with the write pattern, which the type's documented invariance ("one byte at a time gives the same numbers as one call") does not exempt it from. When a multi-byte character straddles a write boundary the range covers only the part that arrived last, or the held-over bytes are charged to the chunk already emitted. `<p>a€b</p>` fed whole reports one chunk 3..8 whose slice is its text; fed 3 bytes at a time it reports **3..6 for the text "a"** - three bytes of range for one byte of text; fed 1 byte at a time, 3..4 "a", 6..7 "€", 7..8 "b", leaving bytes 4 and 5 named by nothing. On that document the slice is not the text at write sizes 1, 2, 3, 5, 6 and text bytes go unnamed at 1, 2. The text is right throughout; only the range moves, and ASCII never shows it. Everything else - element, end tag, comment, doctype - reports the same range at every write size, multi-byte content included, so the way to map text without depending on the write pattern is to take the complement of those ranges and read the bytes from the caller's own copy. That is also lossless on a body a text handler would corrupt: for a 259-byte body holding every byte value, the regions give back its 255 text bytes where the handler's own text reports 511. A trap on the way: filling the gaps between chunks from the input and taking each chunk by slicing the input is a contiguous copy whatever the ranges say, so it reproduces the document even for nonsense ranges and cannot be used as a check. | upstream, gated | `examples/gip/textmap` |
| B194 | The units a rewrite reports do not tile the document: a stray end tag reaches no handler at all. An end tag is observable only through `Element.OnEndTag` (B76), so one with no start tag has no element to register on, and no document-level handler covers it - measured for `</p>`, `</span>`, `</br>`, `</img>`, `</p class=x>`, `</>`, `</circle>` and a trailing `</li>`. The bytes are still written to the output, so reconstructing a document from the ranges reported drops them silently: `<p>a</p></p><b>c</b>` rebuilds as `<p>a</p><b>c</b>`. With every handler the library has registered on `*` and the document written in one call, those are the only unnamed bytes in the documents measured. One space decides visibility: `</x>` is an end tag and invisible, `</ x>` is a bogus comment and named. Separately, a start tag's range is exact and not merely prefixed by `<name` - `<a title="a<b">`, `<p attr=>`, `<p/ >` and `<p a="1"b="2">` all slice byte-for-byte - and the slice is the only way back to the author's spelling, since `<P>` reports `TagName` `p`. | binding (documentation), gated | `examples/gip/locate` |
| B199 | There is no streaming form of `DocumentEnd.Append`: `Element` and `EndTag` each have six `Stream` methods and `DocumentEnd` has none, so a trailing append exists in memory in full. A 12 MB report of a million rows cost **65.5 MB** of allocation through `Append` against **16.0 MB** written to the caller's own sink after `Close`, for byte-identical output (1/1000/100k/1M rows: 1,392/47,808/6,643,200/65,470,544 against 1,032/16,568/1,600,632/16,000,680). The routes differ in two things only: the caller escapes its own content, for which `EscapeText` is documented as exactly what `ContentType` `Text` applies, and it must check `Close` first. A handler error does **not** discard the output - the sink keeps the early-stop prefix, so `before<a href="/">l</a>after` with a failing anchor handler leaves `before` there and a report appended afterwards would hang off a truncated document. | binding (documentation), gated | `examples/gip/tailreport` |
| B196 | Turning the text a rewrite reports into the text a parser reports takes four rules and three different element lists, and the one predicate the library exports answers one of them. Measured against x/net/html for all 144 element names in the HTML index: CR and CRLF become LF in **every** element, no exception; a NUL becomes U+FFFD in exactly the ten `IsRawText` elements and is **dropped** in the other 134; character references decode everywhere except **eight** - `iframe noembed noframes noscript plaintext script style xmp`, which is `IsRawText` minus `textarea` and `title`; and one leading LF is dropped by **`pre`, `listing` and `textarea` only**, not by `xmp`, with a leading CRLF counting as that one newline. So `IsRawText` is exactly right for the NUL rule and wrong for the decode rule, and using it there leaves a title undecoded or corrupts a stylesheet - `<style>a{content:"&amp;"}</style>` says `&amp;`, not `&`. The four differences were already measured in `differential/preprocess_test.go` and `rawtext_test.go` from hand-written tables; what the sweep adds is that those tables are complete. The standard library's `html.UnescapeString` agrees with `x/net/html` on every case that separates decoders, so the conversion needs no dependency. | binding (documentation), gated | `examples/gip/texttruth`, `differential/texttruth_test.go` |
| B198 | A caller slicing its own input at reported offsets while streaming cannot drop its buffer between units. A start tag spans writes and its handler runs after its first bytes were handed over: fed 3 bytes at a time, the handler for `<div id=a>` at offset 0 fires while a caller that kept only the current write holds input from offset **9**, and the element it must slice begins before anything it kept. The safe floor is the end of the last unit any handler reported, since tokens do not overlap - which bounds retention between units by the largest single token instead of by nothing. Measured by building it wrong first: the copy failed at every read size below the section, with the section outside the retained range. With that floor, a 1016-byte section was copied out of documents of 5 KB, 41 KB and 801 KB holding **1072, 1504 and 1408** bytes at peak with 512-byte reads - the section plus a read or two, tracking where the read boundaries fell rather than the document. Second-order: the copy is byte-identical to the source except in a start tag the second pass touched, which is re-serialised per B171, so `id=a class = 'x'  data-k` comes back as `id="a2" class = 'x' data-k`. | binding (documentation), gated | `examples/gip/dupsection` |
| B197 | The final chunk of a text node is **not** always empty, against a documented claim that it "carries no bytes... measured empty in every shape tried". When the node ends with bytes that could not be decoded in the document's encoding, the flagged chunk is the replacement character produced for them: three bytes of text at a zero-width source range, since those bytes are not in the input. Measured for `<p>ab\xe9</p>` as UTF-8, unterminated, in `script`, `style`, `title` and `textarea`, after 100 KB of text, and at write sizes 1, 2, 3, 5, 7 and whole. The same bytes declared `windows-1252` decode, and the chunk is empty again. Two consequences: the documented cost of "at least twice per text node" is also wrong where the node is nothing but a truncated multi-byte sequence - `<p>\xe9</p>` and `<p>\xc3</p>` are one call each, both the node's first chunk and its last, while a standalone invalid byte like `<p>\x80</p>` is still two because it is replaced inside the content chunk - and the handler the old wording invited, acting on the flag without reading its text, drops the character silently. Measured: accumulating without reading the final chunk gives "ab" where reading it gives "ab\ufffd". The input class is exactly the one B-row on lossy text is about, so it is the case a proxy meets. | binding (documentation), gated | `handlercalls_test.go` |
| B12 | Rust builds are not bit-reproducible here: two `native` runs of the same commit produced darwin archives differing by about 1 KB. | upstream | `make verify` is a diff to read, not an equality assertion |
